- Basico
git init
git add *
git commit -a -m "first commit"
git push (-u origin "main (ou master)")

- Em caso de conflito
git pull
*resolva o conflito*
git push

- Para clonar um repositório já existente:
git clone "Url aqui (sem aspas)"